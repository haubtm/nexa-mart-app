export enum EGender {
  MALE = 'MALE',
  FEMALE = 'FEMALE',
}

export enum EUserType {
  CUSTOMER = 'CUSTOMER',
  EMPLOYEE = 'EMPLOYEE',
  MANAGER = 'MANAGER',
  STAFF = 'STAFF',
}

export enum ECustomerType {
  REGULAR = 'REGULAR',
  VIP = 'VIP',
}

export enum EDiscountType {
  FIXED = 'fixed',
  PERCENTAGE = 'percentage',
}
